# Frontend environment variables (Vercel uses these at build time)
# Do not commit real secrets in .env; use this example file.

# Base URL of your backend API (Render URL), e.g.
# https://your-backend.onrender.com
REACT_APP_API_URL=